# Adapter boundary

The adapter may observe lifecycle metadata. It must not launch, retry, approve, reject, resume, kill, submit, or delete workflow execution. Export only the documented public manifest and event fields.
